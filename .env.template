# Firebase Configuration for Secure Deployment
# Copy this file to .env and fill in your actual values

# Get your Firebase API key from:
# Firebase Console > Project Settings > General > Web Apps
FIREBASE_API_KEY=your_actual_firebase_api_key_here

# Firebase Project ID (should be: acim-guide-production)
FIREBASE_PROJECT_ID=acim-guide-production

# Optional: Set to 'production', 'staging', or 'development'
NODE_ENV=production

# Instructions:
# 1. Copy this file: cp .env.template .env
# 2. Replace the placeholder values with your actual Firebase configuration
# 3. NEVER commit the .env file to version control
# 4. Use the secure-deploy.js script for deployment
