[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "{{project}}"
version = "0.1.0"
description = "Generated by Fresh scaffold"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "You" }]

[tool.pytest.ini_options]
addopts = "-q"

